# url link
link = input('link to the page you want to scrape: ')

# looking for
entire = input('are u scraping for the entire page: y,n')

# scraped data format
data_format = input('scraped data format as: (json, txt, html, csv): ')

# scraped data format
file_name = input('Save the file as: ')
